# 智能家居系统用户场景 Benchmark 构建方案

## 一、背景与目标

智能家居系统需要理解和执行用户的自然语言指令，涵盖对不同房间、不同设备的查询与控制操作。为系统化地评估模型/系统的理解与执行能力，需要构建一套覆盖全面、层次清晰的 Benchmark 数据集。

**核心目标：** 通过多维度正交组合，系统化地覆盖智能家居场景中的各类用户意图，量化评估系统在不同复杂度下的表现。

---

## 二、Benchmark 维度体系

### 维度 1：空间范围（Room Scope）

衡量指令涉及的房间数量与指代方式。

| 级别 | 说明 | 示例 |
|------|------|------|
| R1-单房间-显式 | 明确指定一个房间 | "把卧室的灯关了" |
| R2-单房间-隐式 | 未指定房间，需根据上下文/默认推断 | "把灯关了"（用户在客厅） |
| R3-多房间-枚举 | 明确列举多个房间 | "把卧室和客厅的灯都关了" |
| R4-多房间-条件 | 通过条件筛选房间 | "把所有没人的房间灯关掉" |
| R5-全屋 | 针对所有房间 | "全屋灯光关闭" |

### 维度 2：设备范围（Device Scope）

衡量指令涉及的设备数量与类型组合。

| 级别 | 说明 | 示例 |
|------|------|------|
| D1-单设备-显式 | 明确指定一个设备 | "打开客厅的空调" |
| D2-单设备-隐式 | 未指定具体设备，需推断 | "太热了"（推断为开空调） |
| D3-多设备-同类 | 多个相同类型的设备 | "把所有灯都调到50%亮度" |
| D4-多设备-异类 | 多个不同类型的设备 | "关灯并且打开空调" |
| D5-设备组/场景 | 一组预设设备联动 | "开启回家模式"（涉及灯、空调、窗帘等） |

### 维度 3：操作类型（Operation Type）

衡量用户意图的动作性质。

| 级别 | 说明 | 示例 |
|------|------|------|
| O1-状态查询 | 查询设备当前状态 | "客厅空调现在多少度？" |
| O2-简单控制 | 开/关等二值操作 | "把卧室灯打开" |
| O3-参数控制 | 设置具体参数值 | "把空调调到26度" |
| O4-相对控制 | 相对当前值调整 | "把灯调亮一点" |
| O5-复合操作 | 查询+控制混合 | "客厅现在几度？太冷了帮我调高两度" |
| O6-条件/定时控制 | 带触发条件或时间约束 | "如果温度低于20度就打开暖气" / "晚上10点自动关灯" |

### 维度 4：指令复杂度（Instruction Complexity）

衡量自然语言表达的理解难度。

| 级别 | 说明 | 示例 |
|------|------|------|
| C1-直接指令 | 意图、设备、参数均明确 | "把客厅空调设置为制冷模式26度" |
| C2-省略指令 | 省略部分信息，需补全 | "也帮我开一下"（承接上文语境） |
| C3-隐含意图 | 未直接说动作，需推理意图 | "好暗啊"（推断为开灯/调亮） |
| C4-多步指令 | 一句话包含多个顺序动作 | "先关掉客厅灯，然后把卧室空调调到25度，最后拉上窗帘" |
| C5-纠错/修改 | 对前一次操作进行修正 | "不对，我说的是卧室不是客厅" |
| C6-确认/追问 | 系统反问后的用户确认 | 系统："您是要关客厅的灯吗？" 用户："对" |

### 维度 5：歧义与消歧（Ambiguity）

衡量指令中的歧义程度及消歧需求。

| 级别 | 说明 | 示例 |
|------|------|------|
| A1-无歧义 | 指令完全明确 | "把主卧的台灯关掉" |
| A2-设备歧义 | 同一房间有多个同类设备 | "把灯关了"（房间有吸顶灯和台灯） |
| A3-房间歧义 | 设备名称在多个房间存在 | "把空调关了"（多个房间都有空调） |
| A4-意图歧义 | 表述可对应多种操作 | "调一下空调"（调温度？调模式？调风速？） |
| A5-指代歧义 | 代词或指示词不明确 | "把那个也打开"（"那个"指什么？） |

### 维度 6：对话轮次（Dialog Context）

衡量是否依赖多轮对话上下文。

| 级别 | 说明 | 示例 |
|------|------|------|
| T1-单轮 | 单条指令即可完成 | "打开客厅灯" |
| T2-多轮-信息补全 | 需要追问补全信息 | 用户："开灯" → 系统："哪个房间？" → 用户："客厅" |
| T3-多轮-连续操作 | 连续多条指令，存在上下文依赖 | "打开空调" → "调到25度" → "风速调大一点" |
| T4-多轮-跨设备切换 | 对话中切换操作设备 | "客厅灯开一下" → "空调也开一下" → "温度调低点" |

---

## 三、设备类型覆盖

Benchmark 应覆盖主流智能家居设备类型，每类设备有不同的可查询属性和可控参数：

| 设备类型 | 典型操作 | 可查询属性 | 可控参数 |
|----------|----------|-----------|----------|
| **照明** (灯/灯带) | 开关、调亮度、调色温 | 开关状态、亮度、色温、颜色 | 开关、亮度(%)、色温(K)、颜色(RGB) |
| **空调/暖通** | 开关、调温、调模式 | 温度设定、当前温度、模式、风速 | 开关、温度、模式(制冷/制热/除湿)、风速 |
| **窗帘/卷帘** | 开合、调开合度 | 开合状态、开合百分比 | 开/关/停、开合度(%) |
| **影音** (电视/音箱) | 开关、调音量、换频道 | 开关状态、音量、当前频道/源 | 开关、音量、频道、输入源 |
| **安防** (门锁/摄像头) | 查询、布防/撤防 | 锁定状态、布防状态、画面 | 锁/解锁、布防/撤防 |
| **传感器** (温湿度/烟雾) | 查询数值 | 温度、湿度、空气质量、烟雾报警 | 通常仅查询，无控制 |
| **小家电** (扫地机/加湿器) | 开关、模式切换 | 工作状态、电量、模式 | 开关、模式、定时 |

---

## 四、场景矩阵与用例构造策略

### 4.1 正交组合核心矩阵

将六个维度进行正交组合：**空间范围 × 设备范围 × 操作类型 × 指令复杂度 × 歧义程度 × 对话轮次**

完全组合的总量 = 5 × 5 × 6 × 6 × 5 × 4 = **18,000** 种理论组合。

实际构建时不需要覆盖所有组合，采用以下策略精简：

### 4.2 分层采样策略

**第一层：基础能力测试（必选，约 100-150 条）**
- 覆盖所有维度的 Level-1（最简单级别）的完整组合
- 目标：验证系统基本功能的正确性
- 特征：单房间 + 单设备 + 简单控制/查询 + 直接指令 + 无歧义 + 单轮

**第二层：单维度递进测试（必选，约 200-300 条）**
- 固定其他维度为 Level-1，依次将单个维度递进到更高级别
- 目标：隔离测试每个维度上的能力上限
- 例：固定单房间/单设备/简单控制/无歧义/单轮，仅将"指令复杂度"从 C1 递进到 C6

**第三层：典型组合测试（必选，约 300-500 条）**
- 选取高频真实场景的组合，如：
  - 多房间 + 同类设备 + 简单控制（"把所有房间的灯关了"）
  - 单房间 + 异类设备 + 多步指令（"关灯、开空调、拉窗帘"）
  - 隐含意图 + 设备歧义 + 多轮补全（"好热啊" → 需要消歧和推理）
- 目标：评估真实使用中的综合表现

**第四层：边界与鲁棒性测试（可选，约 100-200 条）**
- 异常输入：无效设备名、不存在的房间、超出范围的参数
- 矛盾指令："把空调调到制冷模式然后设置50度"
- 拒绝识别：非智能家居意图的指令
- 方言/口语化表达变体

### 4.3 用例模板

每条 Benchmark 用例应包含以下字段：

```
{
  "id": "B-001",
  "dimensions": {
    "room_scope": "R1",
    "device_scope": "D1",
    "operation_type": "O2",
    "instruction_complexity": "C1",
    "ambiguity": "A1",
    "dialog_turns": "T1"
  },
  "home_config": {
    "rooms": ["客厅", "主卧", "次卧", "厨房", "卫生间"],
    "devices": {
      "客厅": ["吸顶灯", "台灯", "空调", "电视", "窗帘"],
      "主卧": ["吸顶灯", "台灯", "空调", "窗帘"]
    },
    "device_states": {
      "客厅-吸顶灯": {"on": true, "brightness": 80, "color_temp": 4000}
    }
  },
  "dialog": [
    {"role": "user", "content": "把客厅吸顶灯关掉"}
  ],
  "expected_output": {
    "intent": "control",
    "actions": [
      {
        "room": "客厅",
        "device": "吸顶灯",
        "operation": "turn_off",
        "params": {}
      }
    ],
    "response_text": "好的，已为您关闭客厅的吸顶灯。"
  },
  "tags": ["基础", "照明", "开关控制"],
  "difficulty": "easy"
}
```

---

## 五、评估指标体系

### 5.1 意图理解评估

| 指标 | 说明 |
|------|------|
| 意图识别准确率 | 查询 vs 控制 vs 复合，是否正确识别 |
| 设备识别准确率 | 是否正确识别目标设备（Precision / Recall / F1） |
| 房间识别准确率 | 是否正确识别目标房间 |
| 参数提取准确率 | 控制参数（温度值、亮度值等）是否正确提取 |

### 5.2 执行正确性评估

| 指标 | 说明 |
|------|------|
| 动作完全匹配率 (Exact Match) | 生成的 action 序列与标准答案完全一致 |
| 动作部分匹配率 | 设备、房间、操作三要素中的匹配比例 |
| 多设备操作的召回率 | 多设备指令中，实际执行的设备是否全部覆盖 |
| 无误操作率 | 没有对非目标设备执行错误操作 |

### 5.3 对话交互评估

| 指标 | 说明 |
|------|------|
| 消歧合理性 | 面对歧义时是否合理追问（而非瞎猜） |
| 对话轮效率 | 完成任务所需的平均对话轮数 |
| 上下文利用率 | 多轮对话中是否正确利用了上文信息 |
| 回复自然度 | 系统回复的自然语言质量（可用人工评分） |

### 5.4 鲁棒性评估

| 指标 | 说明 |
|------|------|
| 同义表达一致性 | 同一意图的不同表达方式是否得到一致结果 |
| 异常输入处理率 | 面对无效/矛盾指令时的合理拒绝或纠正比例 |
| 不支持操作的识别率 | 正确识别出超出能力范围的请求 |

---

## 六、数据构建流程

```
Step 1: 定义家居环境配置
  └─ 设计 3-5 套典型户型配置（小户型/大户型/别墅等）
  └─ 每套配置包含：房间列表、设备列表、设备初始状态

Step 2: 按维度矩阵生成骨架
  └─ 按分层策略确定需要覆盖的维度组合
  └─ 为每个组合生成结构化的 (房间, 设备, 操作, 参数) 四元组

Step 3: 自然语言化
  └─ 为每个四元组编写 3-5 种不同的自然语言表达
  └─ 覆盖：书面语、口语化、省略式、方言变体
  └─ 可借助 LLM 辅助生成，人工审核

Step 4: 标注与审核
  └─ 标注 expected_output（意图、动作、参数）
  └─ 交叉审核，确保标注一致性
  └─ 标注歧义用例的多个合理答案（允许多正确解）

Step 5: 质量控制
  └─ 维度覆盖率检查：确保每个维度的每个级别都有足够样本
  └─ 设备类型覆盖率检查：确保不遗漏主要设备
  └─ 难度分布检查：easy / medium / hard 比例合理（建议 3:4:3）
```

---

## 七、规模建议

| 层级 | 用例数 | 用途 |
|------|--------|------|
| 基础能力 | 100-150 | 冒烟测试、快速回归 |
| 单维递进 | 200-300 | 能力诊断、瓶颈定位 |
| 典型组合 | 300-500 | 综合评估、版本对比 |
| 边界鲁棒 | 100-200 | 安全性、鲁棒性评估 |
| **总计** | **700-1150** | 完整 Benchmark |

可根据实际资源和迭代需要，优先构建前两层（~400 条），再逐步扩展。

---

## 八、总结：核心维度一览

```
Benchmark = f(空间范围, 设备范围, 操作类型, 指令复杂度, 歧义程度, 对话轮次)

其中：
  空间范围 ∈ {单房间显式, 单房间隐式, 多房间枚举, 多房间条件, 全屋}
  设备范围 ∈ {单设备显式, 单设备隐式, 多设备同类, 多设备异类, 设备组/场景}
  操作类型 ∈ {状态查询, 简单控制, 参数控制, 相对控制, 复合操作, 条件/定时控制}
  指令复杂度 ∈ {直接指令, 省略指令, 隐含意图, 多步指令, 纠错修改, 确认追问}
  歧义程度 ∈ {无歧义, 设备歧义, 房间歧义, 意图歧义, 指代歧义}
  对话轮次 ∈ {单轮, 多轮信息补全, 多轮连续操作, 多轮跨设备切换}
```

通过这六大维度的正交组合与分层采样，可以构建出覆盖全面、层次分明、可量化评估的智能家居 Benchmark 数据集。
