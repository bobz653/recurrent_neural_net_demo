# 客服AI答非所问问题解决方案 - 依赖包

# 核心依赖（必需）
# 注：当前实现主要使用Python标准库，以下是可选的增强依赖

# 如果要使用真实的向量数据库进行RAG（可选）
# faiss-cpu>=1.7.4  # Facebook的向量检索库
# sentence-transformers>=2.2.0  # 文本向量化

# 如果要使用更高级的NLP功能（可选）
# jieba>=0.42.1  # 中文分词
# scikit-learn>=1.0.0  # 文本相似度计算

# 如果要连接真实的LLM API（可选）
# openai>=1.0.0  # OpenAI API
# requests>=2.28.0  # HTTP请求

# 数据处理（推荐）
numpy>=1.21.0

# 日志和监控（推荐）
# loguru>=0.6.0  # 更好的日志库

# 测试（开发环境）
# pytest>=7.0.0
# pytest-cov>=3.0.0

# 注意：
# 1. 当前的演示代码可以直接运行，不需要安装额外依赖
# 2. 集成到生产环境时，根据实际需求安装相应的包
# 3. 如果使用OpenAI或其他商业API，需要配置相应的API密钥
